# Neon PostgreSQL Connection (Direct / Unpooled)
DATABASE_URL=postgresql://<user>:<password>@<ep-hostname>.neon.tech/<dbname>?sslmode=require

# Neon PostgreSQL Connection (Pooled)
DATABASE_URL_POOLED=postgresql://<user>:<password>@<ep-hostname>-pooler.neon.tech/<dbname>?sslmode=require
